# Execute flask virtualenv (Makefile)

```bash
    make run
```` 

# Source virtual env to test python things

```bash
make env
.venv/bin/activate
```

Now your python should point to the virtualenv 
